/**
 * MediConsult - Institutional Node Master
 * This utility now serves as a client-side placeholder.
 * Real-time institutional data is served via the /ai/search-hospital backend endpoint.
 */

export const INSTITUTIONS = [];

export const smartInstitutionSearch = (query) => {
  // Client-side search disabled in favor of Backend Verified Registry
  return [];
};
